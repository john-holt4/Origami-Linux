include:
  - local: .gitlab-ci-common.yml
stages:
  - build
build-test-nvidia-image:
  extends: .bluebuild-job-template
  stage: build
  script:
    - bluebuild build --verbose --rechunk-clear-plan --build-chunked-oci --compression-format zstd --push ./recipes/test-nvidia.yml
